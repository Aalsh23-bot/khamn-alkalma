# خمن الكلمة — خطوات بناء التطبيق بالتفصيل

اتبع هذا الملف من فوق لتحت. لا تحتاج خبرة برمجة لرفع النسخة الجاهزة.
إن غيّرت الكود، نفّذ أيضاً القسم «إعادة البناء بعد التعديل».

**اسم التطبيق:** خمن الكلمة  
**معرّف الحزمة:** `app.khamsa.game`  
**الإصدار:** 1.0.0  
**البناء:** 1

---

## 0) ماذا في المجلد بعد فك الضغط

فك الملف `khamsa-appstore.zip`. يظهر مجلد اسمه `khamsa`.

| الملف / المجلد | الغرض |
|---|---|
| `اقرأني.txt` | ملخص سريع |
| `خطوات-البناء.md` | هذا الدليل |
| `ios/App/App.xcodeproj` | مشروع الآيفون — يُفتح بـ Xcode |
| `android/` | مشروع الأندرويد — يُفتح بـ Android Studio |
| `src/` | كود اللعبة |
| `native/XCODE.md` | نصوص المتجر ولقطات الشاشة |
| `native/store-screenshots/` | صور جاهزة لصفحة المتجر |

انسخ مجلد `khamsa` إلى سطح المكتب أو مجلد العمل، مسار عربي أو مسافات طويلة قد يسبب مشاكل نادرة — الأفضل: `Macintosh HD/Users/اسمك/khamsa`.

---

## 1) ما تحتاجه قبل البناء

### لآيفون ومتجر أبل (لازم ماك)

1. جهاز **ماك**.
2. **Xcode** من متجر أبل (مجاني، حجم كبير — اتركه يثبّت كاملاً).
3. بعد أول فتح لـ Xcode:
   - اقبل الرخصة.
   - Settings → Platforms → ثبّت **iOS**.
4. حساب **Apple ID**.
5. اشتراك **Apple Developer Program** (99 دولار سنوياً) من  
   [developer.apple.com/programs](https://developer.apple.com/programs/)
6. بدون الاشتراك تستطيع تشغيل اللعبة على المحاكي فقط، لا رفع للمتجر.

### لأندرويد ومتجر غوغل (ماك أو ويندوز)

1. **Android Studio** من موقع غوغل.
2. أثناء التثبيت فعّل Android SDK و Platform-Tools.
3. حساب Google.
4. حساب **Google Play Console** (رسوم لمرة واحدة) من  
   [play.google.com/console](https://play.google.com/console)

### اختياري — إن أردت تعديل الكود ثم إعادة البناء

1. **Node.js 22** من [nodejs.org](https://nodejs.org)
2. Terminal (الماك) أو PowerShell (ويندوز)

إن فتحت المشروع الجاهز كما هو، تقدر تتخطى Node وتعمل مباشرة بالقسم 3 أو 7.

---

## 2) تجهيز المشروع على الكمبيوتر (مرة واحدة إن عدّلت الكود)

افتح Terminal داخل مجلد `khamsa`:

```bash
cd ~/khamsa
npm install
npm run native:sync
```

المعنى:

- `npm install` ينزّل المكتبات.
- `npm run native:sync` يبني واجهة اللعبة وينسخها إلى مجلدَي `ios` و `android`.

انتظر حتى تظهر رسالة نجاح بلا خطأ أحمر. بعدها المشروع محدّث.

لفتح البرامج تلقائياً:

```bash
npx cap open ios
npx cap open android
```

---

## 3) بناء نسخة الآيفون وتشغيلها على المحاكي

### 3.1 افتح المشروع

1. افتح **Xcode**.
2. File → Open.
3. اختر الملف: `khamsa/ios/App/App.xcodeproj`
4. انتظر حتى ينتهي فهرسة المشروع أعلى النافذة (قد يأخذ دقيقة).

أو انقر مرتين على `App.xcodeproj`.

### 3.2 اختر الهدف والجهاز

1. في الشريط العلوي يسار زر التشغيل يظهر اسم الهدف: اختر **App**.
2. بجانبه قائمة الأجهزة: اختر مثلاً **iPhone 16** (محاكي).
3. إن لم تظهر محاكيات: Xcode → Settings → Platforms → iOS → Get.

### 3.3 التوقيع (مهم حتى على المحاكي أحياناً)

1. من القائمة اليسرى اضغط أيقونة المشروع الزرقاء **App**.
2. تحت TARGETS اختر **App**.
3. تبويب **Signing & Capabilities**.
4. ضع علامة على **Automatically manage signing**.
5. Team: اختر فريقك (حساب المطوّر). إن لم يظهر: Add Account… وسجّل Apple ID.
6. Bundle Identifier اتركه `app.khamsa.game`.
   - إذا ظهر خطأ «identifier is already in use» غيّره إلى شيء خاص بك مثل `com.yourname.khamsakalima`  
     واحفظ نفس المعرّف لاحقاً في App Store Connect.

### 3.4 تأكد من بيانات النسخة

نفس الصفحة، تبويب **General**:

- Display Name: `خمن الكلمة`
- Bundle Identifier: كما في الأعلى
- Version: `1.0.0`
- Build: `1`
- Minimum Deployments: iOS 15.0
- iPhone Orientation: Portrait فقط (أزل Landscape)

### 3.5 شغّل اللعبة

1. اضغط زر ▶ Play أو اختصار `Cmd + R`.
2. أول مرة ينتظر بناء المحاكي عدة دقائق.
3. تظهر اللعبة: الرئيسية → كلمة اليوم أو المراحل.

إن ظهر خطأ أحمر: اقرأ القسم 10.

### 3.6 التشغيل على آيفون حقيقي

1. وصّل الآيفون بالكابل.
2. على الآيفون: Settings → Privacy & Security → Developer Mode → On (إن طُلب).
3. في Xcode اختر اسم جهازك من قائمة الأجهزة بدل المحاكي.
4. ▶ Play.
5. أول مرة على الجهاز: Settings → General → VPN & Device Management → ثق بالمطوّر.

---

## 4) أرشفة نسخة الآيفون ورفعها لأبل

لا تعمل Archive والمحاكي مختار. لازم جهاز حقيقي أو Generic iOS Device.

1. من قائمة الأجهزة اختر **Any iOS Device (arm64)**.
2. القائمة: **Product → Archive**.
3. انتظر البناء (عدة دقائق). عند النجاح تُفتح نافذة Organizer وفيها الأرشيف.
4. اضغط **Distribute App**.
5. اختر **App Store Connect** → Next.
6. اختر **Upload** → Next.
7. اترك الخيارات الافتراضية (Upload your app’s symbols…).
8. Next → Upload.
9. بعد دقائق تصل رسالة في App Store Connect أو البريد: Processing complete.  
   إن رُفض التوقيع أو المعرّف، راجع 3.3.

---

## 5) إنشاء التطبيق في App Store Connect

ادخل [appstoreconnect.apple.com](https://appstoreconnect.apple.com)

### 5.1 أنشئ التطبيق

1. Apps → زر + → New App.
2. Platforms: iOS.
3. Name: `خمن الكلمة`
4. Primary Language: Arabic.
5. Bundle ID: اختر `app.khamsa.game` (يجب أن يطابق Xcode).
6. SKU: `khamsa001` (أي رمز داخلي لا يراه المستخدم).
7. User Access: Full Access.

إن لم يظهر Bundle ID في القائمة: Certificates, Identifiers & Profiles → Identifiers → + → App IDs → سجّل نفس المعرّف.

### 5.2 معلومات النسخة 1.0.0

بعد أن يظهر البناء تحت **TestFlight** (قد يأخذ 10–30 دقيقة):

1. افتح التطبيق → iOS App → + Version 1.0.0 إن لم تُنشأ.
2. Build: اختر البناء الذي رفعته من Xcode.
3. انسخ النصوص من الأسفل.

**وصف قصير (subtitle / promotional):**  
خمن كلمة عربية فصحى من خمسة أحرف في ست محاولات.

**الوصف:**

```
خمن الكلمة هي نسخة عربية من لعبة تخمين الكلمات.

• خمس خانات وست محاولات
• كلمة فصحى جديدة كل يوم
• مائة مرحلة متتالية
• تلميح واحد لكل جولة
• وضع صعب
• إحصائيات وسلسلة انتصارات
• مشاركة النتيجة كشبكة ملوّنة

ا / أ / إ / آ حرف واحد. ه و ة حرفان مختلفان. ى و ي حرفان مختلفان.

تعمل دون إنترنت بعد التثبيت. لا حسابات ولا إعلانات ولا تتبّع.
```

**الكلمات المفتاحية:**  
`خمن,الكلمة,ورذل,عربي,تحدي,يوميا,حروف,لغز,فصحى,تخمين,مراحل`

**الفئة:** Games → Word  أو Puzzle  
**العمر:** 4+  
**السعر:** مجاني (أو كما تريد)

**لقطات الشاشة:** من مجلد `native/store-screenshots/`  
أبل تطلب مقاسات محددة، أشهرها آيفون 6.7/6.9 إنش. إن لم تطابق، شغّل المحاكي iPhone 16 Pro Max والتقط `Cmd + S`.

**سياسة الخصوصية URL:** أبل تطلب رابطاً. ضع صفحة بسيطة على أي استضافة، أو انسخ نص الإعدادات داخل اللعبة إلى صفحة. النص موجود في التطبيق: الإعدادات → سياسة الخصوصية.

**التشفير:** عند السؤال اختر أن التطبيق لا يستخدم تشفيراً خاصاً (ITSAppUsesNonExemptEncryption = NO وهو مضبوط في المشروع).

### 5.3 أرسل للمراجعة

1. أكمل كل الحقول الحمراء المطلوبة.
2. Add for Review → Submit.
3. المراجعة عادة 24–48 ساعة. إن سألوا: التطبيق حزمة محلية وليست موقعاً خارجياً، لعبة كلمات عربية، تعمل دون شبكة، بلا حسابات ولا تتبّع.

---

## 6) TestFlight (تجربة قبل النشر)

1. في App Store Connect → TestFlight.
2. Internal Testing: أضف بريد فريقك.
3. على الآيفون ثبّت تطبيق TestFlight من المتجر، اقبل الدعوة، ثبّت **خمن الكلمة**.
4. العب كلمة اليوم والمراحل وتأكد أن المشاركة والاهتزاز يعملان.

---

## 7) بناء نسخة الأندرويد

### 7.1 افتح المشروع

1. افتح **Android Studio**.
2. File → Open → اختر مجلد `khamsa/android` (المجلد نفسه وليس الأعلى).
3. عند السؤال Trust Project → Trust.
4. انتظر Gradle Sync في الشريط السفلي حتى ينتهي (أول مرة يحمل أدوات).

إن ظهرت رسالة SDK ناقص: اضغط Install ثم أعد المزامنة.

### 7.2 شغّل على المحاكي

1. Device Manager → Create Device → Pixel 8 → صورة نظام (API 34 مثلاً) → Finish.
2. اختر المحاكي من القائمة العليا.
3. اضغط ▶ Run.
4. تظهر اللعبة.

### 7.3 جهّز مفتاح التوقيع (مرة واحدة مدى الحياة)

بدون هذا المفتاح لا ترفع لغوغل، وضياعه يعني لا تستطيع تحديث التطبيق لاحقاً. احفظه في مكان آمن.

1. Build → Generate Signed App Bundle or APK.
2. Android App Bundle → Next.
3. Create new…  
   - Key store path: مثلاً `khamsa/khamsa-release.jks` (خارج مجلد النسخ الاحتياطي العام إن أمكن).  
   - Password: كلمة مرور قوية.  
   - Alias: `khamsa`  
   - Validity: 25 years.  
   - الاسم والمؤسسة.
4. OK → Next.
5. Build variant: **release**.
6. Finish. الملف الناتج `.aab` يظهر في  
   `android/app/release/` أو المسار الذي يذكره Android Studio.

لا ترفع ملف `.jks` للمتجر ولا تضعه داخل zip عام. احتفظ به ونسخ كلمة المرور.

### 7.4 بيانات النسخة

في `android/app/build.gradle`:

- `applicationId` = `app.khamsa.game`
- `versionCode` = 1   ← زد الرقم واحداً في كل رفع جديد (2 ثم 3…)
- `versionName` = `1.0.0`

---

## 8) رفع الأندرويد إلى Google Play

1. [play.google.com/console](https://play.google.com/console) → Create app.
2. الاسم: `خمن الكلمة` — اللغة العربية.
3. App or game: Game. Free or paid: كما تريد.
4. أكمل الإقرارات: سياسة المحتوى، إعلانات (لا توجد)، الفئة Word / Puzzle، الفئة العمرية.
5. سياسة الخصوصية: رابط صفحة كما في أبل.
6. Store listing: الوصف من القسم 5.2 + أيقونة `resources/icon.png` (1024×1024) + لقطات من `native/store-screenshots/`.
7. Production (أو Testing أولاً) → Create release → ارفع ملف `.aab`.
8. Review → Start rollout to Production.

أول رفع لغوغل قد يأخذ أيام مراجعة. بعدها التحديثات أسرع.

---

## 9) إعادة البناء بعد تعديل الكود

كل مرة تغيّر فيها اللعبة في `src/`:

```bash
cd ~/khamsa
npm install
npm run native:sync
```

ثم:

- **آيفون:** افتح Xcode → زد رقم Build (1→2→3) → Product → Archive → Upload.
- **أندرويد:** زد `versionCode` → Generate Signed Bundle من جديد → ارفع `.aab`.

لا ترفع بناءً بنفس رقم الإصدار السابق.

---

## 10) أخطاء شائعة

**Signing for "App" requires a development team**  
لم تختر Team في Signing & Capabilities. سجّل Apple ID واختر الفريق.

**Failed to create provisioning profile / Bundle ID in use**  
غيّر Bundle Identifier إلى معرّف خاص بك، وأنشئ نفس المعرّف في Apple Developer → Identifiers.

**Archive is grayed out**  
الجهاز المختار محاكي. اختر Any iOS Device (arm64).

**Command PhaseScriptExecution failed**  
من Terminal داخل المجلد: `npm run native:sync` ثم أعد Archive.

**Unable to boot simulator**  
Xcode → Settings → Platforms → تأكد أن iOS مثبّت. ثم Device → Erase All Content على المحاكي.

**Gradle sync failed**  
File → Sync Project with Gradle Files. إن استمر: ثبّت SDK 35 / Build-Tools من SDK Manager.

**AAB not accepted: versionCode already used**  
زد `versionCode` في `android/app/build.gradle`.

**التطبيق يفتح شاشة بيضاء على الجهاز**  
لم تُزامَن ملفات الويب. نفّذ `npm run native:sync` ثم ابنِ من جديد.

**Invalid Swift Package / Capacitor plugins**  
Xcode يحمّل الحزم من الإنترنت أول مرة. File → Packages → Resolve Package Versions، وانتظر الشبكة.

---

## 11) ملخص سريع — آيفون

1. فك الضغط → انسخ `khamsa` للماك.  
2. Xcode → Open → `ios/App/App.xcodeproj`.  
3. Signing: Automatically + Team.  
4. شغّل على محاكي للتأكد (▶).  
5. اختر Any iOS Device → Product → Archive.  
6. Distribute → App Store Connect → Upload.  
7. App Store Connect: أنشئ التطبيق، الصق الوصف، اختر البناء، Submit.

## 12) ملخص سريع — أندرويد

1. Android Studio → Open → مجلد `android`.  
2. ▶ على محاكي.  
3. Build → Generate Signed Bundle → أنشئ `.jks` واحفظه.  
4. ارفع `.aab` في Play Console.  
5. أكمل بيانات المتجر ثم Rollout.

---

نصوص المتجر المختصرة أيضاً في `native/XCODE.md`.  
لقطات الشاشة في `native/store-screenshots/`.
